import ConnectButton from '../ConnectButton';
import { useLogin } from '../../core';

const Plain = () => {
  // %REMOVE_META_CODE
  const {} = useLogin();
  // %REMOVE_META_CODE
  return (
    <>
      {/*<ConnectButton/>*/}
    </>
  );
};

export default Plain;
